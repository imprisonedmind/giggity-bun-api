{
  "name": "bun-mongodb-api",
  "version": "0.1.0",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --hot run index.ts",
    "dbuild": "docker build -t bun-api .",
    "drun": "docker run -p 8080:8080 bun-api"
  },
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mongodb": "^6.9.0",
    "openai": "^4.67.3"
  }
}
